usage: extraction.py [-h] [--all] [--workers WORKERS]

FoxPro to SQLite High Performance Pipeline

options:
  -h, --help            show this help message and exit
  --all                 Process all DBF files
  --workers, -w WORKERS

  use this 
  py extraction.py --all -w 21 for noc database convertion
